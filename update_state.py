import random

def return_updated_state(current_state, activity):

    ## take in a state
    ## take in an activity
    ### make a rules based algorithm to (state) + (activity) -> new_state

    # returning dummy state for now
    foo = ['red', 'blue', 'green', 'yellow']
    return random.choice(foo)
